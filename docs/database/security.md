# Database Security

## タイムアウトの設定

デフォルトが無制限のようだったので設定した。

## RLS

アプリケーションでRLS相当の機能を実現している。

DBのRLSを利用しない理由:

- DDLのメンテコスト増加
- 暗黙的動作が増える
- たまに実行計画に影響を与える
- アプリとDBの往復回数が増える

## credentialsの分離

パスワード等のセンシティブな情報は主要なテーブルに含めず別のテーブルに切り出すことで、不必要にデータを持ち回らないようにした。
