# pgschema
PGDATABASE=test

# app
DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${DB_PORT}/test?sslmode=disable
NODE_ENV=test
PASSWORD_HASH_MEMORY_COST=4096
PASSWORD_HASH_TIME_COST=1
PASSWORD_HASH_PARALLELISM=1
