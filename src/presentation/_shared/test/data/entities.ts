import { entities as refreshTokens } from "./entities/refresh-tokens.ts";
import { entities as todos } from "./entities/todos.ts";
import { entities as users } from "./entities/users.ts";

export const entities = {
  refreshTokens,
  todos,
  users,
};
